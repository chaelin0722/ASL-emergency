//import { saveUserProgress } from './progress';
//import { auth } from './auth';

//export function handleVideoClick(term) {
 //   const userId = auth.currentUser.uid; // Get the current user's ID
   // saveUserProgress(userId, term, true); // Mark the video as watched
//}
